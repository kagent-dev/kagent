// Code generated by weaver from telemetry/registry. DO NOT EDIT.

// Package conv holds the attribute and metric names of the kagent telemetry contract.
package conv

import "go.opentelemetry.io/otel/attribute"

const (
{%- for attr in ctx.attributes %}
	// {{ attr.name }}Key is "{{ attr.key }}". {{ attr.brief }}
	{{ attr.name }}Key = attribute.Key("{{ attr.key }}")
{%- endfor %}
)
{%- for attr in ctx.attributes if attr.members %}

// Values of {{ attr.name }}Key.
const (
{%- for member in attr.members %}
	// {{ attr.name }}{{ member.name }}: {{ member.brief }}
	{{ attr.name }}{{ member.name }} = "{{ member.value }}"
{%- endfor %}
)
{%- endfor %}
{% for metric in ctx.metrics %}
// {{ metric.name }}: {{ metric.brief }}
const (
	{{ metric.name }} = "{{ metric.key }}"
	{{ metric.name }}Unit = "{{ metric.unit }}"
	{{ metric.name }}Description = "{{ metric.brief }}"
)
{% endfor %}
