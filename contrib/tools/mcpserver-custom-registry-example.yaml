apiVersion: kagent.dev/v1alpha3
kind: RemoteMCPServer
metadata:
  name: example-custom-registry
  namespace: kagent
spec:
  url: "http://example-mcp-server.kagent.svc.cluster.local:8080/mcp"
  protocol: STREAMABLE_HTTP
  timeout: 30s
  description: "Example RemoteMCPServer configuration"
