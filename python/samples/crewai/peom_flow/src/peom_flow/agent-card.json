{
  "name": "peom-flow-agent",
  "description": "A flow that generates a peom of a random number of lines.",
  "url": "localhost:8080",
  "version": "0.1.0",
  "capabilities": {
    "streaming": true
  },
  "defaultInputModes": ["text/plain"],
  "defaultOutputModes": ["text"],
  "skills": [
    {
      "id": "generate-peom",
      "name": "Generate a peom",
      "description": "Can generate a peom by providing a topic and sentence count as input",
      "tags": ["creativity", "writing"]
    }
  ]
}
