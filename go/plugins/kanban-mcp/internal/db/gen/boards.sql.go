// Code generated by sqlc. DO NOT EDIT.
// source: boards.sql

package dbgen

import (
	"context"
)

func scanBoard(row interface {
	Scan(dest ...interface{}) error
}) (Board, error) {
	var i Board
	err := row.Scan(
		&i.ID,
		&i.Key,
		&i.Name,
		&i.Description,
		&i.Scope,
		&i.Owner,
		&i.Columns,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Subtasks,
	)
	return i, err
}

const createBoard = `-- name: CreateBoard :one
INSERT INTO kanban.board (key, name, description, scope, owner, columns, subtasks)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, key, name, description, scope, owner, columns, created_at, updated_at, subtasks
`

type CreateBoardParams struct {
	Key         string
	Name        string
	Description string
	Scope       string
	Owner       string
	Columns     []string
	Subtasks    []string
}

func (q *Queries) CreateBoard(ctx context.Context, arg CreateBoardParams) (Board, error) {
	row := q.db.QueryRow(ctx, createBoard,
		arg.Key,
		arg.Name,
		arg.Description,
		arg.Scope,
		arg.Owner,
		arg.Columns,
		arg.Subtasks,
	)
	return scanBoard(row)
}

const upsertBoard = `-- name: UpsertBoard :one
INSERT INTO kanban.board (key, name, description, scope, owner, columns, subtasks)
VALUES ($1, $2, $3, $4, $5, $6, $7)
ON CONFLICT (key) DO UPDATE
SET name        = EXCLUDED.name,
    description = EXCLUDED.description,
    scope       = EXCLUDED.scope,
    owner       = EXCLUDED.owner,
    columns     = EXCLUDED.columns,
    subtasks    = EXCLUDED.subtasks,
    updated_at  = NOW()
RETURNING id, key, name, description, scope, owner, columns, created_at, updated_at, subtasks
`

type UpsertBoardParams struct {
	Key         string
	Name        string
	Description string
	Scope       string
	Owner       string
	Columns     []string
	Subtasks    []string
}

func (q *Queries) UpsertBoard(ctx context.Context, arg UpsertBoardParams) (Board, error) {
	row := q.db.QueryRow(ctx, upsertBoard,
		arg.Key,
		arg.Name,
		arg.Description,
		arg.Scope,
		arg.Owner,
		arg.Columns,
		arg.Subtasks,
	)
	return scanBoard(row)
}

const getBoardByKey = `-- name: GetBoardByKey :one
SELECT id, key, name, description, scope, owner, columns, created_at, updated_at, subtasks FROM kanban.board
WHERE key = $1
`

func (q *Queries) GetBoardByKey(ctx context.Context, key string) (Board, error) {
	row := q.db.QueryRow(ctx, getBoardByKey, key)
	return scanBoard(row)
}

const getBoardByID = `-- name: GetBoardByID :one
SELECT id, key, name, description, scope, owner, columns, created_at, updated_at, subtasks FROM kanban.board
WHERE id = $1
`

func (q *Queries) GetBoardByID(ctx context.Context, id int64) (Board, error) {
	row := q.db.QueryRow(ctx, getBoardByID, id)
	return scanBoard(row)
}

const listBoards = `-- name: ListBoards :many
SELECT id, key, name, description, scope, owner, columns, created_at, updated_at, subtasks FROM kanban.board
ORDER BY created_at
`

func (q *Queries) ListBoards(ctx context.Context) ([]Board, error) {
	rows, err := q.db.Query(ctx, listBoards)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Board
	for rows.Next() {
		i, err := scanBoard(rows)
		if err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
