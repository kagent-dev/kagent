apiVersion: kagent.dev/v1alpha2
kind: MCPServer
metadata:
  name: everything-mcp-server
  namespace: kagent
spec:
  deployment:
    port: 3000
    cmd: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-everything"
  transportType: stdio
