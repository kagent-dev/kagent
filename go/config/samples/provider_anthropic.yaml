apiVersion: kagent.dev/v1alpha2
kind: Provider
metadata:
  name: anthropic-prod
  namespace: kagent
spec:
  type: Anthropic
  endpoint: https://api.anthropic.com
  secretRef:
    name: anthropic-secret
    key: apiKey
