suite: test ui deployment
templates:
  - ui-deployment.yaml
tests:
  - it: should render ui deployment with default values
    template: ui-deployment.yaml
    asserts:
      - isKind:
          of: Deployment
      - equal:
          path: metadata.name
          value: RELEASE-NAME-ui
      - equal:
          path: spec.replicas
          value: 1
      - hasDocuments:
          count: 1

  - it: should render ui deployment with custom replica count
    template: ui-deployment.yaml
    set:
      ui:
        replicas: 2
    asserts:
      - equal:
          path: spec.replicas
          value: 2

  - it: should have correct ui container image
    template: ui-deployment.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[0].name
          value: ui
      - matchRegex:
          path: spec.template.spec.containers[0].image
          pattern: "^cr\\.kagent\\.dev/kagent-dev/kagent/ui:.+"

  - it: should use global tag when set
    template: ui-deployment.yaml
    set:
      tag: "v1.0.0"
    asserts:
      - equal:
          path: spec.template.spec.containers[0].image
          value: cr.kagent.dev/kagent-dev/kagent/ui:v1.0.0

  - it: should have correct ui resources
    template: ui-deployment.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[0].resources.requests.cpu
          value: 100m
      - equal:
          path: spec.template.spec.containers[0].resources.requests.memory
          value: 256Mi
      - equal:
          path: spec.template.spec.containers[0].resources.limits.cpu
          value: 1000m
      - equal:
          path: spec.template.spec.containers[0].resources.limits.memory
          value: 1Gi

  - it: should have correct service account name
    template: ui-deployment.yaml
    asserts:
      - equal:
          path: spec.template.spec.serviceAccountName
          value: RELEASE-NAME-ui

  - it: should have correct container port
    template: ui-deployment.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[0].ports[0].containerPort
          value: 8080

  - it: should set nodeSelector
    set:
      ui:
        nodeSelector:
          role: AI
    asserts:
      - equal:
          path: spec.template.spec.nodeSelector
          value:
            role: AI

  - it: should use default httpGet startup probe
    template: ui-deployment.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[0].startupProbe.httpGet.path
          value: /health
      - equal:
          path: spec.template.spec.containers[0].startupProbe.periodSeconds
          value: 1

  - it: should use default httpGet readiness probe
    template: ui-deployment.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[0].readinessProbe.httpGet.path
          value: /health
      - equal:
          path: spec.template.spec.containers[0].readinessProbe.periodSeconds
          value: 30

  - it: should allow overriding startup probe for IPv6 clusters
    template: ui-deployment.yaml
    set:
      ui:
        startupProbe:
          exec:
            command: ["curl", "-sf", "http://[::1]:8080/health"]
          initialDelaySeconds: 5
          periodSeconds: 3
          failureThreshold: 30
    asserts:
      - equal:
          path: spec.template.spec.containers[0].startupProbe.exec.command
          value: ["curl", "-sf", "http://[::1]:8080/health"]
      - equal:
          path: spec.template.spec.containers[0].startupProbe.periodSeconds
          value: 3
      - notExists:
          path: spec.template.spec.containers[0].startupProbe.httpGet

  - it: should allow overriding readiness probe for IPv6 clusters
    template: ui-deployment.yaml
    set:
      ui:
        readinessProbe:
          exec:
            command: ["curl", "-sf", "http://[::1]:8080/health"]
          periodSeconds: 10
    asserts:
      - equal:
          path: spec.template.spec.containers[0].readinessProbe.exec.command
          value: ["curl", "-sf", "http://[::1]:8080/health"]
      - equal:
          path: spec.template.spec.containers[0].readinessProbe.periodSeconds
          value: 10
      - notExists:
          path: spec.template.spec.containers[0].readinessProbe.httpGet

  - it: should not mount supervisord config by default
    template: ui-deployment.yaml
    asserts:
      - notContains:
          path: spec.template.spec.volumes
          content:
            name: supervisord-config
          any: true
      - notContains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: supervisord-config
          any: true

  - it: should mount custom supervisord config when provided
    template: ui-deployment.yaml
    set:
      ui:
        supervisordConfig: |
          [supervisord]
          nodaemon=true
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: supervisord-config
            configMap:
              name: RELEASE-NAME-ui-supervisord
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: supervisord-config
            mountPath: /etc/supervisor/conf.d/supervisord.conf
            subPath: supervisord.conf
            readOnly: true

  - it: should not mount nginx config by default
    template: ui-deployment.yaml
    asserts:
      - notContains:
          path: spec.template.spec.volumes
          content:
            name: nginx-config
          any: true
      - notContains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: nginx-config
          any: true

  - it: should mount custom nginx config when provided
    template: ui-deployment.yaml
    set:
      ui:
        nginxConfig: |
          pid /tmp/nginx.pid;
          events { worker_connections 1024; }
          http {
            server {
              listen 8080;
              listen [::]:8080;
              location /health { return 200 'OK'; }
            }
          }
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: nginx-config
            configMap:
              name: RELEASE-NAME-ui-nginx
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: nginx-config
            mountPath: /etc/nginx/nginx.conf
            subPath: nginx.conf
            readOnly: true

  - it: should set tolerations
    set:
      ui:
        tolerations:
          - key: role
            operator: Equal
            value: AI
            effect: NoSchedule
    asserts:
      - contains:
          any: true
          path: spec.template.spec.tolerations
          content:
            key: role
            value: AI
            effect: NoSchedule
            operator: Equal